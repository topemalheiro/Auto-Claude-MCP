# Auto Claude Web - Environment Variables
# Copy this file to .env.local and fill in the values.

# ── Cloud Mode (Convex) ──────────────────────────────────────────────
# Set NEXT_PUBLIC_CONVEX_URL to enable cloud mode with Convex backend.
# Leave unset for self-hosted mode using the local Python backend.
# NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# ── Self-Hosted Mode ─────────────────────────────────────────────────
# URL of the local Python backend REST API (self-hosted mode only).
NEXT_PUBLIC_API_URL=http://localhost:8000

# ── Socket.IO (Real-time) ───────────────────────────────────────────
# WebSocket URL for real-time agent/terminal updates (self-hosted mode).
NEXT_PUBLIC_SOCKET_URL=http://localhost:8000

# ── Authentication ───────────────────────────────────────────────────
# Better Auth secret for session signing (cloud mode).
# BETTER_AUTH_SECRET=your-secret-here

# ── General ──────────────────────────────────────────────────────────
# Set to "true" to enable verbose logging in the browser console.
# NEXT_PUBLIC_DEBUG=false
